{
  "dialect": "opx-field-contract/v1",
  "contractId": "modernization-solution-design",
  "title": "Modernization solution design (machine form)",
  "appliesTo": ".modernization/portal/data/json/modernization-solution-design.json",
  "rootPath": "modernization-solution-design",
  "groundedBy": [
    ".github/scripts/shared/verify-step-artifacts.ps1 :: New-ControlPlaneSkeleton (deterministic No-QA writer; authoritative minimum shape)",
    ".github/prompts/05-P1-modernization-solution-design.prompt.md (Step 5 enriched authoring spec)"
  ],
  "authoringNote": "Two legitimate producers write this file: the deterministic No-QA skeleton (New-ControlPlaneSkeleton, which always writes reportId + title) and the Step 5 agent (enriched). This contract MUST pass on the skeleton, so the required floor is intentionally identity-only. The operational 'sections' array is NOT required or type-checked here because its enriched shape is owned by Step 5 and may legitimately vary; Step 8 reads the markdown Solution-Design report by exact path and treats this JSON defensively. Step 5's prompt mandates these same identity fields so the producer and this contract stay aligned.",
  "type": "object",
  "notEmpty": true,
  "required": ["reportId", "title"],
  "fields": {
    "reportId": { "type": "string", "notEmpty": true },
    "title": { "type": "string", "notEmpty": true }
  }
}
